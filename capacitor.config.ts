import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-action-sheet-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
